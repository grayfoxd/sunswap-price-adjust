# SunSwap 调价工具 · 操作手册

面向运维 / 调价人员的操作指引。开发与部署见 [README.md](./README.md)。

---

## 0. 准备工作

1. **安装 TronLink 扩展** 并解锁，确认账户里有少量 TRX 用于支付能量/带宽。
2. **打开页面后顶部网络下拉选好网络**（Mainnet / Nile Testnet）。该选择决定读链路与签名校验依据。
3. **点击右上角「连接 TronLink」**，地址显示绿色 Tag = 已连接。
4. **关键校验**：TronLink 扩展的当前网络必须与页面顶部所选一致。**切换 TronLink 网络后必须刷新页面 (F5)**，否则注入的 `window.tronWeb` 仍指向旧 RPC，所有写操作会失败。代码会在签名前预检 host 并给出明确报错 —— 如看到 "钱包网络与 App 不一致" 提示，按上述步骤刷新即可。

---

## 1. ① 池子信息（查询）

只读，用于确认池子当前状态。

**V3**：两种模式
- `tokens + fee`：填 token0 + token1 + fee（100 / 500 / 1000 / 3000 / 10000），factory.getPool 查池地址
- `直接输入池地址`：粘贴 `T...` base58 池地址

**V4**：两种模式
- `tokens + fee + spacing`：填全 token0 / token1 / fee / tickSpacing / 可选 hooks
- `直接输入 poolId`：粘贴 `0x` + 64 位 hex；该模式下 token 元数据未知，需手动填 dec0 / dec1

点击「查询」后展示：sqrtPriceX96、currentTick、活跃流动性 L、human 价格、fee/tickSpacing。

⚠️ V4 tokens 模式：currency0 / currency1 必须按 EVM 字节序升序，若填反顺序页面会弹「一键互换」按钮，点一下即可。

---

## 2. ② 价格计算器

纯本地计算，不需要联网。`price ↔ sqrtPriceX96 ↔ tick` 三向联动。

输入 token0 decimals、token1 decimals，然后修改任一栏，另外两栏自动同步。常用来：
- 把"我想要 1 USDT ≈ 6.5 TRX" 这样的人类价格 → sqrtPriceX96，用于推价目标
- 反查链上 sqrtPriceX96 是多少 tick / 多少人类价
- 给定 tick 看对齐到 tickSpacing 后是什么价（输入 tickSpacing 会显示对齐前后值）

---

## 3. ③ 推动价格（核心功能）

把池子价格推到指定目标价。流程：

### 3a. 常规路径（目标价不在 tick 边界）

1. 填池子（同 ①），自动加载 PoolStateCard 显示当前 sqrtPrice / tick / L
2. 在「目标价格」区填**人类价** 或 sqrtPriceX96
3. （可选）调整滑点 (bps)
4. 点 **「估算 / 校准」**
   - V3：本地估算 amountIn → 调 Quoter（先 exactInput 试 limit=target，再 exactOutput 反推**精确 amountIn**）→ 显示推荐值
   - V4：本地估算 + Quoter 试探（V4 Quoter 不支持 sqrtPriceLimit，结果仅供参考）
5. 检查 plan 卡片：
   - **方向** zeroForOne (价格下行) / oneForZero (价格上行)
   - **推荐 amountIn (精确)** ← V3 会显示这个，是 Quoter 反推的精确值，使用它**恰好**到达目标价
   - **Quoter 预计 amountOut** + 跨越 tick 数 + 可达性
6. 点 **「执行 V3/V4 swap (UniversalRouter)」** → Modal 二次确认 → TronLink 签名
7. swap 完成后 PoolStateCard 自动刷新，可肉眼对比

### 3b. ⚡ 强制兑换（用于边界场景）

当遇到下面任一情况时，常规路径走不通，使用本 Card：
- pool currentTick 已经在 `MIN_TICK` / `MAX_TICK` 边界
- Quoter 报错（流动性不足、PoolKey 不匹配、fee/spacing 错误等）
- 本地估算返回 0 或异常

**操作**：
1. 同样先在 ① 区填好池子，加载 `current` 状态
2. 滚到页面底部「⚡ 强制兑换」Card
3. **tokenIn** 下拉选 token0 或 token1（标签自动显示 symbol + 地址，方向由此推导）
4. **amountIn** 填**人类单位**（自动按 decimals 转 raw）
5. **amountOutMin** 强烈建议填非 0 防 MEV；留空 = 0 会弹警告条
6. 点 **「强制执行 V3/V4 swap」** → Modal 确认 → 签名

**注意**：
- 强制兑换跳过所有目标价估算，amountIn 由你直接给定，可能把价格推得很远或吃光当前流动性。**amountOutMin 是唯一兜底**
- V3 池子不支持 native TRX（用 WTRX 包装）；V4 native TRX 走 callValue 注入，自动跳过 Permit2
- V4 poolId 直输模式因缺 token 信息，不能用强制兑换 —— 切到 tokens + fee + spacing 模式重新填

---

## 4. ④ 流动性

辅助工具，给定价格区间 + 一边 token 数量，计算 L 与另一边数量。支持单边场景（当前价不在区间内）。

实际 mint / burn 流程的合约调用复杂，目前 UI 仅给参数提示，需在官方 PositionManager 上完成。

---

## 5. 常见问题

### 5.1 "钱包网络与 App 不一致：TronLink 当前 RPC=… App 选的是 …"

TronLink 切网后忘记刷新页面。**解决**：在 TronLink 切到目标网络 → **F5 刷新页面** → 重新连接钱包。

### 5.2 "No contract or not a valid smart contract"

99% 是 5.1 的同类问题（早期没加 host 一致性预检时直接抛这个）。按 5.1 处理。

如果钱包网络确认无误仍报这个错，检查：
- `src/config/networks.js` 里对应网络的合约地址是否有效（可用 `curl POST $RPC/wallet/getcontract -d '{"value":"T...","visible":true}'` 验证 bytecode 是否存在）
- token 地址是否打错（譬如把 mainnet WTRX 复制到 nile 测试）

### 5.3 Quoter 在边界 tick 报错 / amountIn 算出 0

正常情况：池子 currentTick 已经触达 MIN/MAX，没法再朝那个方向推。**解决**：用 ③b 强制兑换在**反方向**送一笔小额 swap 把 tick 推回中段，再走常规路径。

### 5.4 V4 tokens 模式显示 "currency0 与 currency1 顺序反了"

V4 PoolKey 要求 currency0 < currency1 （EVM 字节序）。点 PoolSelector 上的「一键互换」按钮即可。

### 5.5 池子 liquidity = 0

池子已被取空。Quoter 拒绝估算。可以：
- 先用 ④ 注入流动性
- 或换 fee / tickSpacing 找有流动性的同对 pool

### 5.6 推价后实际价格超调

V3 走 SwapRouter 直接调用能用 sqrtPriceLimitX96 限制成交价；但当前 UI 默认走 UniversalRouter 路径，UR 的 `V3_SWAP_EXACT_IN` 不暴露 sqrtPriceLimitX96，amountIn 多给就会超调。靠 Quoter 反推的精确 amountIn + amountOutMin 约束，不能硬限。V4 类似。

**对策**：小步快跑，分多笔小额推。

### 5.7 交易广播成功但实际未上链

检查 Tronscan 看 feeLimit / 能量是否充足。`src/services/router.js` 默认 feeLimit=500_000_000 sun，复杂 V4 路径可能不够，可临时调高。

---

## 6. 一些实用 console 命令

DevTools Console 里跑：

```js
// 查看当前钱包注入的 RPC（用于确认 5.1 那类问题）
window.tronWeb.fullNode.host

// 查某地址在钱包当前网络上是否有合约
await window.tronWeb.trx.getContract('T...')

// 看 force swap 调用上下文（提交前会打印）
// 关键字: [forceSwap]
```
