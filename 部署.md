# 免费部署指引

本项目 `npm run build` 后的 `dist/` 是纯静态资源，可以丢到任意 CDN。下面给出 **Vercel** 和 **GitHub Pages** 两条免费路径，按推荐程度排序。

---

## ✅ 方案 A：Vercel（推荐，零配置）

Vercel 对 Vite + React SPA 自动识别 + 自动 SPA fallback，BrowserRouter 直接可用，不用改一行代码。

### 步骤

1. **推到 GitHub**
   ```bash
   # 在 github.com 上 New repository 后
   git remote add origin git@github.com:<你的账号>/<repo>.git
   git push -u origin main   # 或 dev
   ```

2. **导入到 Vercel**
   - 访问 https://vercel.com，用 GitHub 账号登录
   - `Add New...` → `Project` → 选刚才的 repo → `Import`
   - Framework Preset 应该自动检测为 **Vite**；Build Command / Output Directory 不用改
   - **Root Directory** 留空（如果是 monorepo 子目录就填子目录路径）

3. **环境变量**（可选）
   - Settings → Environment Variables 加：
     - `VITE_DEFAULT_NETWORK` = `nile` 或 `mainnet`
     - `VITE_TRONGRID_API_KEY` = `<你的 key>`（建议加，否则容易被限频）
     - `VITE_MAINNET_RPC` / `VITE_NILE_RPC` = 自定义 RPC（可选）
   - 加完点 `Redeploy`

4. **Deploy** → 等 1~2 分钟 → 拿到 `https://<repo>.vercel.app`

5. **自定义域名**（可选）
   - Settings → Domains → Add → 按提示设 DNS CNAME 即可

### 后续
- 每次 push 到 GitHub，Vercel 自动构建 + 部署
- PR 自动生成 preview URL
- 完全免费，对小型项目流量足够

---

## 方案 B：GitHub Pages

需要做两处调整。最省心的搭配：**HashRouter + GitHub Actions**。

### 第一步：路由改成 HashRouter

GitHub Pages 不支持 SPA fallback（除非用 404.html trick），最简单是把路由换成 hash 模式。URL 会变成 `https://xxx.github.io/repo/#/pool` 这种形式。

编辑 `src/main.jsx`：

```diff
- import { BrowserRouter } from 'react-router-dom'
+ import { HashRouter } from 'react-router-dom'
  ...
-       <BrowserRouter>
+       <HashRouter>
          <App />
-       </BrowserRouter>
+       </HashRouter>
```

> 如果不想用 hash，看本文末尾「附录 1：用 BrowserRouter + 404.html」。

### 第二步：调整 base path

如果部署到 **user/org page**（`<account>.github.io`，根路径）—— `vite.config.js` 现有的 `base: './'` 就够，无需改。

如果部署到 **project page**（`<account>.github.io/<repo>/`，带子路径）—— 同样 `base: './'` 也 OK，因为它是相对路径。

### 第三步：用 GitHub Actions 自动部署

在仓库根建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]   # 改成你的主分支
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          VITE_DEFAULT_NETWORK: nile
          VITE_TRONGRID_API_KEY: ${{ secrets.TRONGRID_API_KEY }}
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - id: deployment
        uses: actions/deploy-pages@v4
```

### 第四步：在 GitHub 仓库设置启用 Pages

- Settings → Pages → **Source** 选 **GitHub Actions**
- 没有这个选项就是仓库还没启用 Pages，旧 UI 的话先选 "Deploy from a branch" → 任意分支 → 保存，然后回来再切到 "GitHub Actions"

### 第五步：触发部署

```bash
git add .github/workflows/deploy.yml src/main.jsx
git commit -m "ci: deploy to github pages"
git push
```

到 Actions 标签页看构建日志，绿勾后访问 `https://<account>.github.io/<repo>/` 即可。

### 环境变量管理
- 公开变量（如 `VITE_DEFAULT_NETWORK`）直接写在 workflow 的 `env:` 里
- 私密变量（如 `VITE_TRONGRID_API_KEY`）在 GitHub 仓库 Settings → Secrets and variables → Actions → New repository secret 创建，然后用 `${{ secrets.XXX }}` 引用

---

## 选哪个？

| 维度 | Vercel | GitHub Pages |
|---|---|---|
| 配置复杂度 | 零配置 | 需改路由 + 写 workflow |
| SPA fallback | ✅ 自动 | ❌ 需 HashRouter 或 404 trick |
| URL 美观 | `/pool` | `/#/pool`（hash 模式） |
| 自定义域名 | ✅ 免费 | ✅ 免费 |
| PR preview | ✅ 自动 | ❌ |
| Edge CDN | ✅ 全球 | ✅ 全球 |
| 流量限额 | 100GB/月 (Hobby) | 100GB/月软限 |

**建议**：用 Vercel。GitHub Pages 适合"我已经有 GitHub 流程不想再加一个平台"的场景。

---

## 附录 1：GitHub Pages + BrowserRouter（保留干净 URL）

如果坚持要用 BrowserRouter，在 `public/404.html` 创建一个文件：

```html
<!DOCTYPE html>
<html>
<head><meta charset="utf-8" /></head>
<body>
<script>
  // GitHub Pages SPA fallback
  // 把任意 404 路径重定向到 index.html 并带上原 path
  var path = location.pathname.replace(/^\/[^/]+/, '') + location.search + location.hash
  location.replace('/' + (location.pathname.split('/')[1] || '') + '/?p=' + encodeURIComponent(path))
</script>
</body>
</html>
```

然后在 `index.html` 顶部加一段把 `?p=` 转回正常 path 的脚本。完整方案参考 [rafrex/spa-github-pages](https://github.com/rafgraf/spa-github-pages)（搜 "spa-github-pages"）。

不推荐为此投入精力 —— 用 HashRouter 一行改动就解决。

---

## 附录 2：部署前的检查清单

- [ ] `vite build` 本地能成功跑（确认 `dist/` 生成）
- [ ] `vite preview` 在 4173 端口能看到完整页面
- [ ] TronLink 连接测试通过（注意：localhost / 自定义域名的连接行为可能不同）
- [ ] `src/config/networks.js` 里相应网络的合约地址有效
- [ ] 如果改了 router，确认 4 个页面 `/pool` `/calculator` `/push` `/liquidity` 都能正常加载
- [ ] 如果用了 API Key，确认它没硬编码在源码里（应该走环境变量）
